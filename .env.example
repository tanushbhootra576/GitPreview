# GitPreview Environment Configuration

# To increase your GitHub API rate limit from 60 to 5000 requests per hour,
# add your GitHub Personal Access Token here:

# VITE_GITHUB_TOKEN=your_github_personal_access_token_here

# How to create a GitHub Personal Access Token:
# 1. Go to https://github.com/settings/tokens
# 2. Click "Generate new token (classic)"
# 3. Give it a descriptive name like "GitPreview"
# 4. No special scopes are needed for public repositories
# 5. Click "Generate token"
# 6. Copy the token and paste it above (replace the placeholder)

# Note: Keep this token secure and never commit it to version control!